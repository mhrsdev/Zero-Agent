[pytest]
# Single source of pytest configuration. pytest's discovery order reads
# pytest.ini before pyproject.toml and stops at the first file that yields
# config, so settings duplicated in pyproject.toml would be silently dropped.
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
