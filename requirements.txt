telethon==1.44.0
aiogram==3.30.0
# Security floor: aiohttp <3.13.4 has known PYSEC advisories; aiogram 3.30
# allows <3.15 so this only lifts the resolver off vulnerable old releases.
aiohttp>=3.13.4,<3.15
PyYAML==6.0.2
aiosqlite==0.20.0
pydantic==2.9.2
python-dotenv==1.2.3
# Required by zoneinfo-based timezone validation on Windows (no system tz db).
tzdata>=2024.1
